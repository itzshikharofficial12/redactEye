# Face Detection Models (`models/face`)

This directory holds metadata, input scaling parameters, and runtime configurations for client-side face detection models used in visual redaction.

## Guidelines
- Do not commit raw weights or binary files.
- Document confidence thresholds, non-maximum suppression (NMS) parameters, and detection precision/recall in `docs/models/`.
