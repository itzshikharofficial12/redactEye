# OCR Models (`models/ocr`)

This directory holds metadata, runtime configurations, and tokenizer/vocabulary definitions for in-browser Optical Character Recognition (OCR).

## Guidelines
- Do not commit raw weights or binary files.
- Document character sets, inference latencies, memory footprint, and WebGPU/WASM compatibility in `docs/models/`.
