# Vision Models (`models/vision`)

This directory holds metadata, ONNX export configurations, and preprocessing specifications for vision perception models running client-side.

## Guidelines
- Do not commit raw weights or binary files.
- Document model architectures, input/output tensors, quantization levels (e.g., INT8/FP16), and execution provider benchmarks in `docs/models/`.
