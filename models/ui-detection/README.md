# UI Detection Models (`models/ui-detection`)

This directory holds metadata, class mappings, and runtime specifications for UI element detection models (identifying buttons, inputs, icons, forms, and interactive widgets).

## Guidelines
- Do not commit raw weights or binary files.
- Document label taxonomies, bounding box normalization rules, and latency characteristics in `docs/models/`.
