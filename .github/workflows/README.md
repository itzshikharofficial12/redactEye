# GitHub Actions Workflows (`.github/workflows`)

This directory is reserved for continuous integration, linting, type-checking, and automated testing workflows.
Workflows will be introduced incrementally as project packages are implemented.
