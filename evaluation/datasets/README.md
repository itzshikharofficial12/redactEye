# Benchmark Datasets (`evaluation/datasets`)

This directory holds benchmark datasets, ground-truth annotations, and dataset loader manifests.

## Guidelines
- Synthetic and publicly licensed benchmark subsets only.
- Do not store raw heavyweight dataset binaries; use loader scripts and manifests.
