# DOM Test Fixtures (`test-fixtures/dom`)

This directory is reserved for synthetic DOM trees, HTML snippets, and accessibility trees used to test DOM extraction and coordinate calculation.

## Rules
- Synthetic HTML only.
- Include varied layouts (forms, multi-column tables, dynamic modals, iframes) with mocked data.
