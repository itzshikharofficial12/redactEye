# Sanitized Test Fixtures (`test-fixtures/sanitized`)

This directory is reserved for expected baseline sanitized outputs (both masked images and redacted JSON envelopes) used to verify that redaction engines produce deterministic, leak-free results.

## Rules
- Contains ground truth redacted artifacts generated from synthetic inputs.
