# Agent Tasks Test Fixtures (`test-fixtures/agent-tasks`)

This directory is reserved for end-to-end multi-step synthetic task definitions (e.g., "fill a mock shipping form", "navigate mock pagination", "select options in a mock settings panel") used to test the agent reasoning and execution loop.
