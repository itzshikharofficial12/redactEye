# PII Test Fixtures (`test-fixtures/pii`)

This directory is reserved for synthetic test strings, form payloads, and mocked documents containing known PII categories.

## Rules
- Strictly synthetic test strings (e.g., standard Luhn-valid mock credit cards like 4111-XXXX-XXXX-XXXX, standard 555 mock phone numbers, example.com mock emails, mock government IDs).
- Absolutely no authentic individual data.
